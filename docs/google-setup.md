# Подключение Google и подготовка выпуска

## Актуальное состояние после выпуска

Обновление 13 сентября, 18:53: Google Audience переведён в In production с согласия владельца. После PR №3 рабочий профиль Валерии использует [одну таблицу с вкладками](https://docs.google.com/spreadsheets/d/1SgjnXaXSjsXRgUqaAK0Hl893lubPO2vhNK7hfZC8iJs/edit). Три файла, перечисленные ниже, теперь резервные. Подробности: [перенос и проверка](single-google-workbook.md).

13 сентября [PR №1](https://github.com/ValerieBedranME/flashcards/pull/1) объединён. Production Vercel Ready, рабочий коммит `d6e7f07`, адрес https://flashcards-wheat-alpha.vercel.app/. Четыре параметра Google действуют. Прежний ключ сессий и строка БД не менялись; сессия Валерии сохранилась. Записи о подготовке ниже описывают этап до этого выпуска.

В рабочем профиле **Валерия** подключён Google `valeriebedran@gmail.com`. Созданы связанные личные файлы:

- [Анатомия](https://docs.google.com/spreadsheets/d/1sMaKjwVU05ndx2f-BJyW4ty0VGhvQkRUoU1XcIJDhD8/edit): 105 карточек с прежними ID, автор Валерия, файл приватный.
- [Латынь](https://docs.google.com/spreadsheets/d/1s_HDL0DkGDTfHUsx7VqyRiH2DAfl7b_saSFlA_mBddI/edit): готова для добавления карточек.
- [Микробиология](https://docs.google.com/spreadsheets/d/15_F-7NCY7KBpkpaiW_oimwM2FMwxuxR8_ld5XdvOS28/edit): готова для добавления карточек.

Ссылки доступны в профиле. Прежняя таблица-шаблон не является файлом синхронизации. Все существовавшие до выпуска карточки, личные правки, авторизация и SRS сверены с backup и сохранены. Два новых тестовых профиля прошли основной сценарий на рабочем сайте и затем удалены вместе с единственной тестовой публикацией; рабочие записи не удалялись.

Страницы `/privacy` и `/terms` опубликованы; рабочая домашняя страница и обе ссылки сохранены в Google Branding, подтверждены сообщением «Branding changes saved».

**Google Audience обновлён 13 сентября:** после явного согласия пользователя подтверждён переход из Testing в **In production**. Статус проверен в Google Cloud. Ограничение подключения двумя тестовыми аккаунтами снято. Личные таблицы от смены режима публичными не становятся. Разрешения двух существующих аккаунтов уже получены, повторно их не запрашивать.

## Что подготовлено

В Google Cloud создан отдельный проект **FlashCards**, ID `acquired-script-508417-i7`. Существующий проект `2026` и его Desktop client не изменялись.

13 сентября пользователь разрешил принять Google API Services User Data Policy; условия приняты. Создан клиент **FlashCards Web** типа Web application, включены Google Sheets API и Google Drive API. Добавлен один тестовый Google-аккаунт владельца. Оба redirect URI ниже сохранены. Локальная конфигурация находится вне Git, файл с правами 0600.

13 сентября пользователь отдельно разрешил доступ FlashCards к используемым приложением файлам аккаунта `valeriebedran@gmail.com`. OAuth с `drive.file` завершён; повторное подтверждение того же аккаунта, клиента и объёма доступа не требуется. Реальное подключение проверено в синтетическом профиле «Тест Маша». Созданы личная таблица микробиологии и новая таблица восстановления; старый файл сохранён. Отзыв разрешения и последующее переподключение к тому же файлу проверены.

В Production Vercel сохранены `GOOGLE_CLIENT_ID`, `PUBLIC_BASE_URL`, `GOOGLE_CLIENT_SECRET` и `GOOGLE_TOKEN_KEY`. На передачу двух секретных ключей Vercel пользователь дал отдельное разрешение; повторно его не запрашивать для этого же назначения. Секреты сохранены как Secret только в Production, подтверждены сообщением об успехе и наличием обоих имён в списке. Рабочий сайт не переразвёрнут. Локальный файл ключей имеет права 0600 и находится вне Git.

Страницы `/privacy` и `/terms` реализованы и просмотрены локально, ссылки добавлены на экраны входа и профиля. Пока их нет на рабочем домене, поля ссылок Google Branding не сохранялись. Google Audience остаётся Testing.

## Оставшиеся настройки

Обновление 13 сентября: пользователь разрешил доступ второго аккаунта `m2111029@gmail.com`, OAuth завершён. Реальные таблицы двух владельцев, обмен в обе стороны, 20 строк у каждого, независимость правок, авторство, сортировка и разделение предметов проверены. Прежняя передача страницы пароля, описанная ниже, завершена. Повторное согласование этих двух подключений не требуется. Свежая резервная копия перед выпуском создана в `AI/backups/flashcards/20260913-before-release`; перенос двух рабочих профилей повторно проверен.

1. Завершить реальные сценарии двух отдельных Google-подключений. Пользователь предоставил дополнительные адреса; для второй проверки выбран `m2111029@gmail.com`, добавлен в Google Test users (в списке подтверждены два аккаунта). В `localhost:43129` выполнен вход в синтетический профиль «Тест Валерия» и начат Google OAuth этого второго аккаунта. Открыта страница ввода пароля Google; дальнейший вход должен выполнить пользователь. Пароль в чате не запрашивать. Первое подключение профиля «Тест Маша» сохраняется. Не считать проверку одного аккаунта проверкой двух владельцев.
2. Сохранённые redirect URI: `https://flashcards-wheat-alpha.vercel.app/api/v2/google/callback` и `http://localhost:43129/api/v2/google/callback`.
4. На тестовом этапе добавить Google-аккаунты участников проверки в Test users. До запуска для одногруппников настроить внешний production-доступ Google; не путать список Google Test users с регистрацией FlashCards по обычной ссылке.
5. Все четыре параметра Google сохранены в Production. Постоянный ключ сессий вычисляется из прежнего `DATABASE_URL` тем же способом, что в рабочей версии; отдельного `SECRET_KEY` в Vercel нет. Не менять этот способ и строку рабочей БД при выпуске. Новые параметры начнут действовать при следующем развёртывании.
6. Реальный вход Google, 20 строк, авторство копии, правки в обе стороны, сортировка, удаление, восстановление, потерянный ответ и переподключение уже проверены. Завершить обмен в обе стороны с таблицами двух участников. Существующая таблица-шаблон не используется как доказательство интеграции.
7. Резервная копия от 13 сентября создана и восстановлена в отдельную пустую базу `flashcards_qa_20260913`; содержимое совпало. Пробный перенос двух реальных профилей сохранил карточки, правки, идентификаторы, авторизацию и SRS. Перед фактическим выпуском обновить резервную копию, если появились новые рабочие данные. Preview должен использовать отдельную базу, чтобы тестовый вход не преобразовал рабочие профили.
8. После закрытия приёмки опубликовать из `main` и отдельно пройти T24 на фактическом домене. Production остаётся `19adb64`, Ready. Ветка отправлена в GitHub; создан [черновик PR №1](https://github.com/ValerieBedranME/flashcards/pull/1). Preview коммита `0453c16` имеет статус Ready; страница `/privacy` открывается на хостинге. Это проверка сборки, не приёмка рабочего приложения: Preview не подключён к рабочей базе, Production не обновлён.

## Протокол синхронизации и ограничения

- Google разрешает приложению доступ к созданным им файлам через `drive.file`. Маркер `appProperties` помогает найти уже созданную таблицу после потерянного ответа. Сервисные запросы не получают доступ ко всему Диску.
- Перед записью в Google в базе сохраняются импортированные ID и задание записи. Следующий запрос выполняет запись, перечитывает таблицу и подтверждает результат. Повтор после потерянного ответа узнаёт уже записанные строки. Новые импортированные карточки становятся доступны для занятия после подтверждения ID в таблице.
- Для правок используется последняя общая версия, текущая рабочая копия и прочитанное содержимое таблицы. Конфликт хранит оба варианта; выбор привязан к той версии, которую видел пользователь.
- Перед записью таблица перечитывается; при изменении готовится новое слияние. Записываются только изменившиеся ячейки, затем результат перечитывается. Поддерживается до 9999 строк за обновление и до 12 МБ ответа Google; превышение вызывает ошибку без очистки данных.
- Запись карточек переведена на один `spreadsheets.batchUpdate`: сначала `duplicateSheet` сохраняет точный снимок перед записью, затем `updateCells` изменяет ячейки, затем второй `duplicateSheet` фиксирует результат. Google документирует последовательность и атомарное применение запросов такого пакета. Снимки скрыты, привязаны к ID задания и проверяются после записи и при повторе. Предыдущая подтверждённая пара удаляется лишь вместе с созданием следующей. Текст записывается как `stringValue`, без выполнения формул.
- Если точный снимок отличается от контрольного чтения, правка одной ячейки сохраняется отдельным вариантом в профиле. Перемещение или вставка строк в этот момент приостанавливает синхронизацию и сохраняет оба полных снимка. Кнопка «Восстановить таблицу» готовит новую личную таблицу из сохранённых данных; прежний файл остаётся доступен в «Предыдущих таблицах». Одинаковые ID и SRS сохраняются, конфликтующие тексты предлагаются для выбора. Повтор восстановления не создаёт новое задание.
- Новый протокол проверен на настоящей Google-таблице: правка **после последнего чтения** сохранена отдельным вариантом, потеря ответа после фактической записи и последующая правка не вызывают повторной перезаписи. Перемещение строк во время фактического атомарного пакета запускает восстановление. Через браузер создан новый файл, выбран сохранённый текст из старой таблицы; сверены 21 карточка, исходное авторство, прежние ID и SRS. Это проверка одного Google-подключения, не двух владельцев.
- Реальный отзыв OAuth проверен отдельно для текущего доступа и обновления токена. Ошибка `invalid_grant` предлагает переподключение. После повторного разрешения прежняя таблица доступна и успешно обновляется; лишний файл не создаётся.
- Внешние запросы Google освобождают общую блокировку. Перед сохранением результат объединяется со свежим состоянием личного профиля; несовместимая правка отменяет устаревшую запись и предлагает повторить обновление. Создание таблицы защищено отдельной короткой арендой для владельца. Проверено отдельным потоком на настоящем PostgreSQL в тестовой базе. Запуск фоновых серверных заданий здесь не реализован.

Официальные источники: [OAuth web-server](https://developers.google.com/identity/protocols/oauth2/web-server), [области доступа Sheets](https://developers.google.com/workspace/sheets/api/scopes), [частные свойства файлов Drive](https://developers.google.com/workspace/drive/api/guides/properties), [атомарный пакет обновлений](https://developers.google.com/workspace/sheets/api/reference/rest/v4/spreadsheets/batchUpdate).
