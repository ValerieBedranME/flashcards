# Результаты проверки — 12 сентября 2026

Статус: **локальная реализация, приёмка не завершена, публикации не было**. Ветка `feat/personal-decks-google-sheets`. Работа разрешена пользователем; повторное утверждение плана или защиты не требуется.

## Среды

- Автоматические проверки: Flask test clients, временные JSON-файлы, чистые функции модели, имитация Google Sheets и соединения PostgreSQL. 46 проверок проходят. Это не проверка настоящего Google или PostgreSQL.
- Браузер: настоящий Flask на `127.0.0.1:43129` и `localhost:43129`, разные cookies. Отдельные синтетические профили «Тест Валерия» и «Тест Маша». Для двух правок одного профиля использовались две вкладки `localhost`.
- Рабочий Vercel просмотрен без изменений: Ready, `main`, коммит `19adb64`. Новый код на хостинг не отправлялся.
- В Google Cloud создан проект FlashCards; форма Google Auth заполнена до принятия условий. На этом шаге ожидается ответ пользователя. Настоящие OAuth-токены или связанные таблицы новой версии пока не создавались.

## Согласованные сценарии

| Проверка | Результат | Подтверждение и границы |
| --- | --- | --- |
| T01 | Пройдена локально | Три предмета и дерево видны в браузере; одинаковые темы разных предметов разделены в тестах. |
| T02 | Пройдена локально | Согласованный пример даёт ровно 4 / 3 / 2 карточки к повторению; корзина исключена. |
| T03 | Пройдена локально | Тот же пример даёт 9 / 6 / 4 карточки в режиме «Все». |
| T04 | Пройдена локально | Граница срока, будущие и неоценённые карточки, прежний SRS; в браузере оценка «Знаю» сохранена. |
| T05 | Пройдена локально | Пустая микробиология, первая тема, набор и карточка созданы через интерфейс. |
| T06–T10 | Не выполнены с настоящим Google | Имитационные тесты: 20 строк, повтор, сортировка, импорт, отложенная запись и подключение после создания карточек. Нужны два настоящих Google-подключения. |
| T11 | Пройдена локально | Неопубликованный набор закрыт; явная публикация появляется в библиотеке второго профиля. |
| T12 | Не выполнена полностью | В браузере копия получена другим профилем, автор сохранён, правка не меняет оригинал. Экспорт автора проверен на имитации; настоящая таблица ещё не проверена. |
| T13 | Пройдена в API | Новая публикация имеет другой ID; изменение и удаление оригинала не меняют личную копию и прогресс. |
| T14 | Не выполнена с настоящим Google | Повтор копирования и потерянный ответ записи проверены имитацией: лишних карточек и строк нет. |
| T15 | Не выполнена полностью | Браузерная корзина и восстановление работают; удаление строки Sheets проверено только имитацией. |
| T16 | Не выполнена полностью | Две вкладки сохранили оба текста; выбранный вариант затем виден в наборе. Имитация Google показывает обе версии. Окно прямой правки той же ячейки между чтением и записью остаётся открытым техническим вопросом. |
| T17 | Пройдена в локальных API | Изолированные профили и проверки версий. Отдельный поток другого профиля завершил запись во время имитированного ожидания Google; его правки сохранены. Нужна проверка на настоящем PostgreSQL. |
| T18 | Не выполнена полностью | Имитации: ошибки чтения, повреждённый заголовок, повтор ID, потерянный ответ, изменение между подготовкой и записью. Состояние повтора сохраняется при 503, неожиданный 500 откатывается. Реальный отзыв доступа и сбой БД не выполнялись. |
| T19 | Пройдена локально | Два профиля зарегистрированы по обычному адресу. API проверяют чужие ID, пользовательский параметр, CSRF, выход и недопустимый предмет. |
| T20 | Пройдена локально | Перезапуск сервера и перезагрузка не потеряли карточки и прогресс. Вторая сессия получает пустой профиль до копирования; API проверяют повторный вход. |
| T21 | Не выполнена на рабочей копии БД | Синтетический перенос, повтор переноса, шифрование и сверка содержимого проходят. `restore-empty` отказывается перезаписывать непустую базу. Реальный backup и restore PostgreSQL ещё нужны. |
| T22 | Не выполнена полностью | Просмотрены настольный экран, реальные 320 и 360 px в тёмной теме; при 320 px ширина диалога 288, горизонтальной прокрутки нет. Светлая тема просмотрена на локальной QA-странице с принудительным CSS color-scheme: light (360 px), проверены занятие и оценка. Нужен обход экранов настоящего Google-подключения. |
| T23 | Не выполнена с настоящей таблицей | Разметка, кавычки и переносы сохранены и показаны текстом в браузере; RAW-запись формулы проверена имитацией. |
| T24 | Не выполнена | Выпуск допускается после закрытия оставшихся проверок; рабочий сайт не обновлён. |

## Дополнительные проверки защиты

OAuth привязан к профилю, использует одноразовый state и PKCE; повтор callback не получает доступ. Тесты подтверждают шифрование токенов и отсутствие секретов в bootstrap. Старые изменяющие API не позволяют обойти проверки версий для подключённой таблицы. Выбор спорной правки содержит отпечаток увиденных вариантов; новая правка требует заново посмотреть варианты.

Новые импортированные строки не попадают в занятие до подтверждения ID в таблице. Повтор импорта после изменения строки не оставляет лишнюю карточку. Добавление позже ещё одной строки с таким же текстом создаёт новую карточку; одинаковый текст не считается идентификатором. Взятую копию можно дополнить строками в Sheets с сохранением исходного автора; неоднозначные названия наборов вызывают явную ошибку.

## Что осталось до выпуска

1. Завершить Google OAuth после согласия на условия сервиса и пройти реальные сценарии с таблицами.
2. Закрыть окно одновременной правки одной ячейки; текущая защита ловит правки до контрольного чтения и после записи, но не гарантирует сохранение неизвестного текста, перезаписанного между ними. Для полной гарантии нужен протокол, при котором приложение не перезаписывает редактируемую пользователем ячейку, либо журнал, фиксирующий такую правку. Одобрение пользователя на проектирование защиты уже получено.
3. Проверить параллельную нагрузку на настоящем PostgreSQL. Ожидание Google уже вынесено за общую блокировку: независимый поток другого профиля завершает запись во время сетевого ожидания, а последующее сохранение не стирает его данные.
4. Сделать рабочий backup, пробный перенос и восстановление; завершить интерфейсные проверки, затем выпуск и T24.

Подробности настройки: [Google и выпуск](google-setup.md). Непроверенные части не считаются пройденными.

Дополнительно проверены короткая аренда создания таблицы (повторный запрос не создаёт второй файл), сохранение ID после ошибки настройки и повторное использование этого файла.

Последний браузерный повтор: конфликт автоматически открывает два варианта, выбор проверяется по отпечатку версии и сохраняется; ошибок console нет. В светлой теме у получателя сохранено «Не уверен: 1», у автора осталось «Знаю: 1». Тестовая смена color-scheme сделана только в локальном скрипте предпросмотра, настройки пользователя не изменялись.
